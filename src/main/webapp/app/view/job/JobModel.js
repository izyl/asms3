Ext.define('asms3.view.job.JobModel', {
	extend : 'Ext.app.ViewModel',
	alias : 'viewmodel.job',

	// Just some data to seed the process. This might be pulled from a cookie or
	// other
	// in a real app.

});